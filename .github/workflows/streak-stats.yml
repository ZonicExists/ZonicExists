name: Generate Streak Stats

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  streak:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate streak stats
        uses: DenverCoder1/github-readme-streak-stats@main
        with:
          options: user=${{ github.repository_owner }}&background=0A0A0A&border=262626&stroke=F5F5F5&ring=F5F5F5&fire=F5F5F5&currStreakNum=F5F5F5&sideNums=A3A3A3&currStreakLabel=F5F5F5&sideLabels=A3A3A3&dates=A3A3A3&hide_border=false
          path: profile/streak.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit streak stats
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile/streak.svg
          git diff --quiet && git diff --staged --quiet || git commit -m "Update streak stats"
          git push
          
